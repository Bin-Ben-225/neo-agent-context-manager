You are working in a project prepared by NACM.

First read `.agent/sessions/context_pack.md`.
{% if task | default("") %}
Current task: {{ task }}
{% endif %}
{% if relation_index_available is defined %}
Relation index: {{ "available" if relation_index_available else "not available" }}
High confidence files: {{ high_confidence_count | default(0) }}
Medium confidence files: {{ medium_confidence_count | default(0) }}
Low confidence files: {{ low_confidence_count | default(0) }}
{% endif %}
Prioritize High Confidence files from the context pack.
{% if relation_index_available | default(false) %}
Use relation index links to check related source and tests before broad searching.
{% endif %}
Do not scan the whole repository by default.
Do not read forbidden paths listed in the context pack.
Do not commit `.agent/`.
Do not create or modify the repository root AGENTS.md.
Before editing, provide a short plan.
If the task requires broad changes, enter Plan Mode first.
When finished, remind the user to run `nacm done`.
